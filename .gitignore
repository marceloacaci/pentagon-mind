# Logs
assets/download.log

# Dependências (não usadas no build, mas por segurança)
node_modules/

# OS / editor
.DS_Store
Thumbs.db
*.swp

# Ambiente
.env
.env.local

# Artefatos de build (gerados pelo pipeline, nunca versionados)
pagefind/
docs/uml/*.png
docs/uml/*.svg

# PENTAGON-MIND watcher runtime state
.pm_watch.log
.pm_watch.pid

# PENTAGON-MIND watcher runtime state
.pm_watch.log
.pm_watch.pid
.pm_watch.lock
